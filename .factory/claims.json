[
  {
    "id": "demo-sandbox",
    "claim": "Try the five-file sample without saving it with your local folder review",
    "where": "landing action, /demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh 390×844 browser context; seed a private real IndexedDB review; enter ?demo=1 in one click; assert a sample row is in the first viewport, then reset, exit, and verify demo-record deletion"
  },
  {
    "id": "approval-required",
    "claim": "A file moves only after you check its proposed destination",
    "where": "landing, workbench, terms, README",
    "test": "npm run test:e2e -- --grep @claim:approval-required",
    "sandbox": "fresh /demo; assert all sample approvals are unchecked; check one and assert the count"
  },
  {
    "id": "displayed-bulk-controls",
    "claim": "Bulk approval changes only displayed rows",
    "where": "workbench bulk controls",
    "test": "npm run test:e2e -- --grep @claim:displayed-bulk-controls",
    "sandbox": "101-file read-only preview; approve displayed 100; reveal row 101 and assert it remains unchecked"
  },
  {
    "id": "review-persistence",
    "claim": "Your approval and destination edits in a saved folder review survive a reload on this device",
    "where": "workbench, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:review-persistence",
    "sandbox": "fresh real sample; edit approval, bucket, and name; reload; assert all three values"
  },
  {
    "id": "imported-receipt-export",
    "claim": "An imported undo receipt stays available to export after undo",
    "where": "receipt status message",
    "test": "npm run test:e2e -- --grep @claim:imported-receipt-export",
    "sandbox": "seed an imported-style receipt with no plan rows; reload; assert receipt heading and JSON/CSV controls"
  },
  {
    "id": "undo-retry",
    "claim": "Undo never overwrites an existing original path and can be retried after you remove that blocker",
    "where": "receipt undo confirmation, terms, README",
    "test": "npm run test:unit -- -t @claim:undo-retry",
    "sandbox": "in-memory filesystem; block original path, verify failed undo, remove blocker, and retry"
  },
  {
    "id": "local-only",
    "claim": "Sample file details stay in your browser",
    "where": "landing, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "fresh /demo; intercept every request during approval and edit; assert no third-party origin"
  },
  {
    "id": "offline-reload",
    "claim": "The app shell reloads offline after the first visit",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; wait for service worker; go offline; reload the home screen"
  },
  {
    "id": "deterministic-routes",
    "claim": "File type and modified year determine the proposed destination",
    "where": "workbench, README",
    "test": "npm run test:unit -- -t @claim:deterministic-routes",
    "sandbox": "deterministic file facts; assert extension classification and destination"
  },
  {
    "id": "reversible-move",
    "claim": "A move copies, verifies, avoids collisions, and can be undone",
    "where": "landing, terms, README",
    "test": "npm run test:unit -- -t @claim:reversible-move",
    "sandbox": "in-memory filesystem; verify collision copy bytes, source removal, and undo restoration"
  },
  {
    "id": "receipt-csv",
    "claim": "Receipts export as CSV",
    "where": "receipt controls, landing, README",
    "test": "npm run test:unit -- -t @claim:receipt-csv",
    "sandbox": "manifest with quoted path and error; assert CSV header and escaped row"
  },
  {
    "id": "receipt-json",
    "claim": "Receipts export their recorded file actions as JSON",
    "where": "receipt controls, landing, README",
    "test": "npm run test:e2e -- --grep @claim:receipt-json",
    "sandbox": "fresh browser context and one-file writable directory fixture; complete the move, download JSON, parse it, and assert the recorded action"
  },
  {
    "id": "real-file-locality",
    "claim": "Selected file details stay in this browser during preview, move, receipt export, and undo",
    "where": "landing, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:real-file-locality",
    "sandbox": "mock local directory; request log across choose, move, exports, and undo contains only same-origin requests"
  },
  {
    "id": "browser-capabilities",
    "claim": "Writable folder choice appears when supported; other browsers can preview and export a plan",
    "where": "workbench, README",
    "test": "npm run test:e2e -- --grep @claim:browser-capabilities",
    "sandbox": "feature-present and feature-absent browser contexts; assert chooser state, preview, and JSON download"
  },
  {
    "id": "storage-boundary",
    "claim": "Demo, real folder review, and Pro license storage use separate documented keys",
    "where": "privacy, README, demo documentation",
    "test": "npm run test:e2e -- --grep @claim:storage-boundary",
    "sandbox": "demo plus real sample and fixture license; enumerate IndexedDB and namespaced localStorage keys"
  },
  {
    "id": "no-tracking-runtime",
    "claim": "The product loads no analytics, third-party scripts, or CDN fonts",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:no-tracking-runtime",
    "sandbox": "request log across home, demo, privacy, and terms; assert every runtime request is same-origin"
  },
  {
    "id": "installable",
    "claim": "Supporting browsers receive the manifest and service worker needed to install Triagebox",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:installable",
    "sandbox": "fresh Chromium context; validate the linked manifest, required icons and standalone start URL, CDP manifest errors, and active controlling service worker"
  },
  {
    "id": "free-limit",
    "claim": "Free runs show a 100-file move limit; a verified $19 Pro license removes it",
    "where": "upgrade, terms, README",
    "test": "npm run test:e2e -- --grep @claim:free-limit",
    "sandbox": "101-file preview plus recorded valid license response; assert free cap message and unlocked Pro state"
  },
  {
    "id": "checkout-origin",
    "claim": "Checkout and license verification use Sociobot",
    "where": "upgrade, terms, README",
    "test": "npm run test:e2e -- --grep @claim:checkout-origin",
    "sandbox": "assert checkout href and record fixture license verification request origin"
  },
  {
    "id": "permission-on-action",
    "claim": "Triagebox asks for a folder only after you choose a folder",
    "where": "workbench, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:permission-on-action",
    "sandbox": "picker spy; assert zero calls on load and one after Choose a folder"
  },
  {
    "id": "receipt-original-timestamp",
    "claim": "Receipts record each file’s original timestamp",
    "where": "receipt, terms, README",
    "test": "npm run test:unit -- -t @claim:receipt-original-timestamp",
    "sandbox": "fixture receipt; assert exact source timestamp in JSON data and CSV"
  },
  {
    "id": "recursive-inventory",
    "claim": "Triagebox lists files in nested folders with their relative paths",
    "where": "README",
    "test": "npm run test:unit -- -t @claim:recursive-inventory",
    "sandbox": "nested in-memory directory; assert each file appears once with its full relative path"
  },
  {
    "id": "plan-import",
    "claim": "An exported review plan restores destination edits and approves only files whose path, size, and date still match",
    "where": "folder controls, README",
    "test": "npm run test:e2e -- --grep @claim:plan-import",
    "sandbox": "fresh browser context; import the recorded three-file plan into a two-file writable fixture; assert the exact file is approved, the changed file is unapproved, the missing file is reported, and destination edits return"
  }
]
